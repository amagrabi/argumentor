[pytest]
log_file = pytest.log
log_file_level = DEBUG
# The app imports its own modules flat (`from models import User`), so src has to
# be importable as a root, not just as a package.
pythonpath = . src